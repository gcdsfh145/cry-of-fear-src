name: Build Android APK

on:
  push:
    branches:
      - main
      - master
      - dev
      - 'feature/**'
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Apply Build Fix Patches
        run: |
          if [ -d ".github/patches" ]; then
            for patch in .github/patches/*.patch; do
              echo "Applying $patch..."
              git apply "$patch"
            done
          fi

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Build Android APK (Release & Debug)
        shell: pwsh
        run: |
          ./build_android.sh -Configuration Release -Abi arm -NdkVersion "26.1.10909125" -AndroidNdkRoot "$ANDROID_HOME/ndk/26.1.10909125" -AndroidSdkRoot "$ANDROID_HOME"

      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: cry-of-fear-android-apk
          path: out/android/xash3d-cof.apk
